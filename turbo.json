{
    "$schema": "https://turborepo.com/schema.json",
    "tasks": {
        "build": {
            "cache": true,
            "inputs": [ "src/**" ],
            "outputs": [ "dist/**" ]
        },
        "test": {
            "cache": true,
            "inputs": [ "src/**" ],
            "outputs": [ "dist/**" ]
        }
    }
}