{
  "name": "bleed-believer",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "scripts": {
    "test": "mocha -r ts-node/register -r tsconfig-paths/register ./packages/*/src/**/*.test.ts",
    "start": "ts-node -r tsconfig-paths/register ./packages/test/src",
    "prepare": "npm run clean && npm run build",
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "clean": "rm -r -f ./packages/*/dist && rm -f ./*.tsbuildinfo && rm -f ./packages/*/*.tsbuildinfo"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sleep-written/bleed-believer.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sleep-written/bleed-believer/issues"
  },
  "homepage": "https://github.com/sleep-written/bleed-believer#readme",
  "workspaces": [
    "packages/command",
    "packages/architecture-test"
  ],
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.1.0",
    "@types/node": "^17.0.14",
    "chai": "^4.3.6",
    "mocha": "^9.2.0",
    "ts-node": "^10.4.0",
    "tsconfig-paths": "^3.12.0",
    "typescript": "^4.5.5"
  }
}
