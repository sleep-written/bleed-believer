{
  "name": "@examples/commander",
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "node --run clean && node --run bleed -- build --config ./tsconfig.build.json",
    "bleed": "node ../../packages/cli/dist/cli.js"
  },
  "dependencies": {
    "@bleed-believer/commander": "*",
    "chalk": "^5.6.2",
    "tslog": "^4.10.2"
  },
  "devDependencies": {
    "@bleed-believer/cli": "*",
    "typescript": "^5.9.3"
  },
  "imports": {
    "#root/*.ts": "./src/*.ts",
    "#root/*.js": "./dist/*.js"
  }
}
